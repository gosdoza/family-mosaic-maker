# /results 頁最小驗收清單

**版本**: v1.0.0  
**最後更新**: 2025-11-09

本文档列出 `/results` 页面的最小验收清单，包括 Loading/Empty/Error 三态验收和画廊显示要点。

## 📋 Loading 狀態驗收

### 加載中顯示

- [ ] 顯示加載動畫（`Loader2` 旋轉圖標）
- [ ] 顯示加載文字（"Loading results..." 或 "Loading your results..."）
- [ ] 加載動畫居中顯示
- [ ] 加載期間顯示 Navigation 和 Footer
- [ ] 加載動畫顏色為 `text-primary`
- [ ] 加載文字顏色為 `text-muted-foreground`

### 加載行為

- [ ] 頁面首次加載時顯示 Loading 狀態
- [ ] API 請求期間保持 Loading 狀態
- [ ] 加載完成後正確切換到其他狀態

## 📋 Empty 狀態驗收

### 空狀態顯示

- [ ] 顯示空狀態標題（"No results found"）
- [ ] 顯示空狀態描述（"It looks like there are no images for this job ID. Please try generating again."）
- [ ] 顯示重試按鈕（"Try Again" 或導向 `/generate`）
- [ ] 空狀態居中顯示
- [ ] 空狀態使用 `ErrorState` 組件
- [ ] 空狀態顯示 Navigation 和 Footer

### 空狀態觸發條件

- [ ] `results` 為 `null` 時顯示空狀態
- [ ] `results.images.length === 0` 時顯示空狀態
- [ ] 空狀態提供重試或重新生成選項

## 📋 Error 狀態驗收

### 錯誤顯示

- [ ] 顯示錯誤標題（"Failed to load results" 或 "Error Loading Results"）
- [ ] 顯示錯誤訊息（具體錯誤描述）
- [ ] 顯示重試按鈕（"Try Again" 或 `router.refresh()`）
- [ ] 錯誤狀態居中顯示
- [ ] 錯誤狀態使用 `ErrorState` 組件
- [ ] 錯誤狀態顯示 Navigation 和 Footer

### 錯誤處理

- [ ] API 請求失敗時顯示錯誤狀態
- [ ] 網絡錯誤時顯示錯誤狀態
- [ ] 超時錯誤時顯示錯誤狀態
- [ ] 錯誤狀態提供重試功能
- [ ] 錯誤訊息清晰易懂

## 📋 畫廊顯示要點

### 頁面標題和描述

- [ ] 顯示頁面標題（"Your Family Mosaic is Ready!" 或 "Your Family Mosaics"）
- [ ] 顯示頁面描述（"Here are the beautiful images generated for your family." 或 "Beautiful AI-generated family moments ready to download and share"）
- [ ] 標題使用漸變色（`bg-gradient-to-r from-primary to-accent`）
- [ ] 標題和描述居中顯示

### 支付狀態顯示

- [ ] **已付費狀態**:
  - [ ] 顯示 "Paid ✅" 徽章（綠色背景，`bg-green-500/20`）
  - [ ] 顯示 `Check` 圖標
  - [ ] 徽章使用 `data-testid="results-paid-badge"`

- [ ] **未付費狀態**:
  - [ ] 顯示未付費提示（"Premium unlocks HD, no watermark."）
  - [ ] 顯示 `Lock` 圖標
  - [ ] 顯示 "Upgrade Now" 或 "Unlock HD" 按鈕
  - [ ] 按鈕導向 `/pricing?job=${id}`
  - [ ] 使用 `data-testid="results-unpaid-banner"` 或 `data-testid="results-unpaid-status"`

### 畫廊網格佈局

- [ ] 使用響應式網格佈局（`grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`）
- [ ] 圖片卡片間距合適（`gap-6`）
- [ ] 圖片卡片使用 `Card` 組件
- [ ] 圖片卡片有圓角（`rounded-xl`）
- [ ] 圖片卡片有陰影效果（`shadow-lg hover:shadow-xl`）
- [ ] 圖片卡片有懸停效果（`hover:scale-105`）

### 圖片顯示

- [ ] 圖片使用 `AspectRatio` 組件（比例 1:1）
- [ ] 圖片使用 `Image` 或 `img` 組件
- [ ] 圖片使用 `object-cover` 類別
- [ ] 圖片有適當的 `alt` 屬性
- [ ] 圖片優先級設置正確（前 3 張使用 `priority={true}`）
- [ ] 圖片尺寸設置正確（`sizes` 屬性）

### 未付費圖片遮罩

- [ ] 未付費圖片顯示模糊效果（`blur-sm` 或 `opacity-75 blur-sm`）
- [ ] 未付費圖片顯示灰度效果（`grayscale`）
- [ ] 未付費圖片顯示水印覆蓋層（"PREVIEW" 文字）
- [ ] 水印文字旋轉（`rotate-[-20deg]` 或 `transform -rotate-45`）
- [ ] 水印文字半透明（`opacity-30` 或 `text-white/30`）
- [ ] 水印覆蓋層使用 `data-testid="watermark-overlay"`

### 懸停交互

- [ ] 圖片卡片懸停時顯示操作按鈕
- [ ] 懸停時顯示漸變覆蓋層（`bg-gradient-to-t from-black/70`）
- [ ] 懸停時顯示下載和分享按鈕
- [ ] 按鈕使用圓角（`rounded-full`）
- [ ] 按鈕有適當的圖標和文字

### 下載按鈕

- [ ] **已付費狀態**:
  - [ ] 顯示 "Download HD" 按鈕
  - [ ] 顯示 `Download` 圖標
  - [ ] 按鈕可點擊
  - [ ] 點擊後觸發下載（`/api/download?job=${id}&i=${imageId}`）
  - [ ] 下載中顯示 "Downloading..." 和 `Loader2` 動畫
  - [ ] 使用 `data-testid="btn-download-hd"`

- [ ] **未付費狀態**:
  - [ ] 顯示 "Download HD" 按鈕（帶鎖圖標）
  - [ ] 顯示 `Lock` 圖標
  - [ ] 按鈕禁用或導向 `/pricing?job=${id}`
  - [ ] 點擊後顯示升級提示或導向定價頁面

### 分享按鈕

- [ ] 顯示分享按鈕（`Share2` 圖標）
- [ ] 按鈕使用 `outline` 變體
- [ ] 點擊後使用 Web Share API（如果可用）
- [ ] 不支持時回退到複製鏈接到剪貼板
- [ ] 分享成功後顯示 Toast 通知

### 其他功能

- [ ] 顯示 "Generate More" 按鈕（導向 `/generate`）
- [ ] 按鈕使用 `Sparkles` 圖標
- [ ] 按鈕使用 `outline` 變體
- [ ] 按鈕使用圓角（`rounded-full`）

## 📋 響應式設計驗收

### 移動端（< 768px）

- [ ] 畫廊使用單列佈局（`grid-cols-1`）
- [ ] 標題和描述字體大小適配
- [ ] 按鈕大小適配
- [ ] 間距適配

### 平板端（768px - 1024px）

- [ ] 畫廊使用兩列佈局（`sm:grid-cols-2`）
- [ ] 標題和描述字體大小適配
- [ ] 按鈕大小適配

### 桌面端（> 1024px）

- [ ] 畫廊使用三列佈局（`lg:grid-cols-3`）
- [ ] 標題和描述字體大小適配
- [ ] 按鈕大小適配
- [ ] 最大寬度限制（`max-w-6xl`）

## 📋 無障礙性驗收

### 鍵盤導航

- [ ] 圖片卡片可通過 Tab 鍵聚焦
- [ ] 聚焦時顯示焦點環（`focus-within:ring-2 focus-within:ring-primary`）
- [ ] Enter/Space 鍵可觸發下載或升級操作

### ARIA 標籤

- [ ] 圖片卡片使用 `role="article"`
- [ ] 圖片卡片使用 `aria-label` 描述
- [ ] 按鈕使用 `aria-label` 描述
- [ ] 支付狀態使用 `role="status"` 和 `aria-label`
- [ ] 水印覆蓋層使用 `aria-hidden="true"`

### 語義化 HTML

- [ ] 使用適當的 HTML 標籤（`<main>`, `<section>`, `<article>`）
- [ ] 使用適當的標題層級（`<h1>`, `<h2>`）
- [ ] 使用適當的列表標籤（如需要）

## 📋 性能驗收

### 圖片加載

- [ ] 前 3 張圖片使用 `priority={true}` 優先加載
- [ ] 圖片使用適當的 `sizes` 屬性
- [ ] 圖片使用適當的 `loading` 屬性（如需要）

### 渲染性能

- [ ] 頁面加載時間 < 2 秒
- [ ] 圖片加載時間 < 3 秒
- [ ] 無明顯卡頓或延遲

## 📋 測試標識符驗收

### 測試 ID

- [ ] `data-testid="results-page"`（主容器）
- [ ] `data-testid="results-paid-badge"`（已付費徽章）
- [ ] `data-testid="results-unpaid-banner"`（未付費橫幅）
- [ ] `data-testid="results-unpaid-status"`（未付費狀態）
- [ ] `data-testid="unlock-hd-cta"`（解鎖 HD 按鈕）
- [ ] `data-testid="watermark-overlay"`（水印覆蓋層）
- [ ] `data-testid="btn-download-hd"`（下載 HD 按鈕）
- [ ] `data-testid="results-gallery"`（畫廊容器）

## 📋 驗收命令

```bash
# 無
```

## 📋 驗收標準

### 必須通過（P0）

- [ ] Loading 狀態正確顯示
- [ ] Empty 狀態正確顯示
- [ ] Error 狀態正確顯示
- [ ] 畫廊正確顯示圖片
- [ ] 支付狀態正確顯示
- [ ] 下載按鈕功能正常
- [ ] 分享按鈕功能正常

### 應該通過（P1）

- [ ] 響應式設計正確
- [ ] 無障礙性符合標準
- [ ] 性能指標達標
- [ ] 測試標識符完整

### 可以通過（P2）

- [ ] 動畫效果流暢
- [ ] 交互體驗良好
- [ ] 視覺效果精美

## 📋 更新日誌

- **v1.0.0** (2025-11-09): 初始版本，定義 `/results` 頁面最小驗收清單



